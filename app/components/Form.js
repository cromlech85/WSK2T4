import React from 'react';
import FormContainer from '../containers/FormContainer';

var Form = React.createClass({
    render: function () {
        return (
            <FormContainer />
        )
    }
});
module.exports = Form;
